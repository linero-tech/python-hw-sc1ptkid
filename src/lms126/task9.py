from to_do import TODO


def task9():
    return """
    START    
    INPUT read user input from card
        COMPUTE Determine password match
        IF match
        OUTPUT print balance on card
    INPUT user input withdraw amount  
        COMPUTE determine balance
        IF match
        Calculate add (New balance)
        THEN withdraw money
    ENDIF
    
    """
